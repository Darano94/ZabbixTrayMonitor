<Window x:Class="ZabbixTrayMonitor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ZabbixTrayMonitor"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        Title="Zabbix Tray Monitor" 
        Height="450" 
        Width="800"
        ShowInTaskbar="False"
        WindowState="Minimized"
        Visibility="Hidden">

    <Window.Resources>
        <!-- Eigenes Tray-Kontextmenü, damit es dem gewählten Dark-/Light-Theme folgt. -->
        <Style x:Key="TrayContextMenuStyle" TargetType="{x:Type ContextMenu}">
            <Setter Property="Background" Value="{DynamicResource WindowBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="3" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ContextMenu}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <StackPanel IsItemsHost="True"
                                        KeyboardNavigation.DirectionalNavigation="Cycle" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TrayMenuItemStyle" TargetType="{x:Type MenuItem}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="MinHeight" Value="27" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type MenuItem}">
                        <Border x:Name="MenuItemRoot"
                                Background="{TemplateBinding Background}"
                                CornerRadius="3"
                                SnapsToDevicePixels="True">
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"
                                              ContentSource="Header"
                                              RecognizesAccessKey="True" />
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="MenuItemRoot"
                                        Property="Background"
                                        Value="{DynamicResource HoverBackgroundBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TrayMenuSeparatorStyle" TargetType="{x:Type Separator}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Separator}">
                        <Border Height="1"
                                Margin="6,3"
                                Background="{DynamicResource DividerBrush}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <tb:TaskbarIcon 
            x:Name="TrayIcon" 
            IconSource="/Assets/tray-unknown.ico"
            ToolTipText="Zabbix Tray Monitor"
            TrayLeftMouseUp="TrayIcon_ShowProblems_Click"
            TrayRightMouseDown="TrayIcon_RightMouseDown"
            PreviewTrayToolTipOpen="TrayIcon_PreviewTrayToolTipOpen"
            PreviewTrayToolTipClose="TrayIcon_PreviewTrayToolTipClose">
            <tb:TaskbarIcon.TrayToolTip>
                <Border Background="#EB202020"
                        BorderBrush="#78FFFFFF"
                        BorderThickness="1"
                        CornerRadius="4"
                        Padding="10,8">
                    <TextBlock x:Name="TrayToolTipTextBlock"
                               Text="Zabbix Tray Monitor&#x0a;---------&#x0a;Initialisiere..."
                               Foreground="White"
                               FontFamily="Segoe UI"
                               FontSize="12"
                               TextWrapping="Wrap"
                               MaxWidth="520" />
                </Border>
            </tb:TaskbarIcon.TrayToolTip>

            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu Style="{StaticResource TrayContextMenuStyle}">
                    <MenuItem Header="Aktualisieren"
                              Style="{StaticResource TrayMenuItemStyle}"
                              Click="TrayIcon_Refresh_Click" />
                    <MenuItem Header="Probleme anzeigen"
                              Style="{StaticResource TrayMenuItemStyle}"
                              Click="TrayIcon_ShowProblems_Click" />
                    <Separator Style="{StaticResource TrayMenuSeparatorStyle}" />
                    <MenuItem Header="Einstellungen"
                              Style="{StaticResource TrayMenuItemStyle}"
                              Click="TrayIcon_Settings_Click" />
                    <MenuItem Header="Info"
                              Style="{StaticResource TrayMenuItemStyle}"
                              Click="TrayIcon_Info_Click" />
                    <Separator Style="{StaticResource TrayMenuSeparatorStyle}" />
                    <MenuItem Header="Beenden"
                              Style="{StaticResource TrayMenuItemStyle}"
                              Click="TrayIcon_Exit_Click" />
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
    </Grid>
</Window>
 